{% load static %}
<!doctype html>
<html lang="pt-br">
<head>
  <meta charset="utf-8" />
  <title>Confirmar exclusÃ£o â {{ obj.participante.nome }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    :root{ --ink:#0f172a; --muted:#64748b; --line:#e5e7eb; --bg:#f6f8fb; --danger:#dc2626; --danger-2:#b91c1c; }
    body{ background:var(--bg); color:var(--ink); }
    .nav-pro{ background:linear-gradient(90deg,#0b1020,#111a3a); }
    .navbar .btn{ font-size:.85rem; padding:.35rem .6rem; }
    .navbar-brand{ font-size:1rem; }
    .card-clean{ border:1px solid var(--line); border-radius:.9rem; background:#fff; box-shadow:0 10px 24px rgba(17,24,39,.08); }
    .sec-title{ font-size:.9rem; color:var(--muted); text-transform:uppercase; letter-spacing:.04em; margin:0 0 .75rem; font-weight:700; }
    .text-muted-2{ color:var(--muted); }
    .badge-soft{ background:#eef2f7; color:#334155; border:1px solid var(--line); }
    .box-dash{ border:1px dashed var(--line); border-radius:.75rem; padding:.75rem; background:#fff; }
    .alert-soft-danger{ background: linear-gradient(180deg, rgba(239,68,68,.10), transparent); border:1px solid rgba(239,68,68,.35); color:#b91c1c; border-radius:.75rem; }
  </style>
</head>
<body>
<nav class="navbar nav-pro navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="{% url 'inscricoes:admin_paroquia_painel' %}">Painel da ParÃ³quia</a>
    <div class="d-flex align-items-center gap-2 ms-auto">
      <a class="btn btn-sm btn-light" href="{% url 'inscricoes:evento_participantes' obj.evento.id %}">â Participantes</a>
    </div>
  </div>
</nav>

<main class="container py-3 py-lg-4">
  <div class="row g-3">
    <section class="col-lg-9 mx-auto">
      <div class="card-clean p-3 mb-3">
        <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
          <div>
            <h1 class="h6 m-0"><i class="fa-solid fa-triangle-exclamation me-1 text-danger"></i> Confirmar exclusÃ£o</h1>
            <div class="small text-muted-2">Esta aÃ§Ã£o Ã© permanente e nÃ£o poderÃ¡ ser desfeita.</div>
          </div>
          <div>
            <a class="btn btn-sm btn-outline-secondary" href="{% url 'inscricoes:evento_participantes' obj.evento.id %}">
              <i class="fa-solid fa-arrow-left"></i> Voltar
            </a>
          </div>
        </div>
      </div>

      <section class="card-clean p-3">
        <h2 class="sec-title"><i class="fa-solid fa-circle-info me-1"></i> Detalhes da inscriÃ§Ã£o</h2>
        {% if obj %}
        <div class="box-dash mb-3">
          <div class="row g-3">
            <div class="col-md-6">
              <div class="small text-muted-2">Participante</div>
              <div class="fw-semibold">{{ obj.participante.nome }}</div>
            </div>
            <div class="col-md-6">
              <div class="small text-muted-2">Evento</div>
              <div class="fw-semibold">{{ obj.evento.nome }}</div>
            </div>
            <div class="col-md-6">
              <div class="small text-muted-2">ID</div>
              <div class="fw-semibold">#{{ obj.id }}</div>
            </div>
            <div class="col-md-6">
              <span class="badge rounded-pill badge-soft mt-4"><i class="fa-solid fa-bomb me-1"></i> ExclusÃ£o permanente</span>
            </div>
          </div>
        </div>
        {% endif %}

        <div class="alert alert-soft-danger mb-3" role="alert">
          <i class="fa-solid fa-skull-crossbones me-2"></i>
          Ao excluir, dados vinculados a esta inscriÃ§Ã£o poderÃ£o ser removidos e nÃ£o serÃ¡ possÃ­vel restaurÃ¡-los.
        </div>

        <form method="post" class="mt-2">
          {% csrf_token %}
          <div class="form-check mb-3">
            <input class="form-check-input" type="checkbox" id="ack">
            <label class="form-check-label" for="ack">
              Confirmo que desejo <strong>excluir permanentemente</strong> esta inscriÃ§Ã£o e compreendo que esta aÃ§Ã£o nÃ£o pode ser desfeita.
            </label>
          </div>

          <div class="d-flex gap-2 flex-wrap">
            <a class="btn btn-outline-secondary" href="{% url 'inscricoes:evento_participantes' obj.evento.id %}">
              <i class="fa-solid fa-circle-xmark"></i> Cancelar
            </a>
            <button class="btn btn-danger" type="submit" id="btnDelete" disabled>
              <i class="fa-solid fa-trash"></i> Sim, excluir
            </button>
          </div>
        </form>
      </section>

      <footer class="py-3">
        <div class="d-flex flex-wrap justify-content-between align-items-center">
          <small>Â© {% now "Y" %} Admin Geral â Todos os direitos reservados</small>
          <small class="text-muted-2">AÃ§Ã£o crÃ­tica: revise antes de confirmar</small>
        </div>
      </footer>
    </section>
  </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
(function(){
  const ack = document.getElementById('ack');
  const btn = document.getElementById('btnDelete');
  if(ack && btn){ ack.addEventListener('change', () => { btn.disabled = !ack.checked; }); }
})();
</script>
</body>
</html>
